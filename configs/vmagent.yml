global:
  scrape_interval: 10s
  external_labels:
    cluster: 'test-cluster'
    replica: '0'

scrape_configs:
  - job_name: 'metrics_load'
    static_configs:
      - targets: ['metrics_load:9100']
    scrape_interval: 10s

